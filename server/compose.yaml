name: yaplingo

services:
  server:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./server:/server/server
    models:
      - llm
    tty: true

models:
  llm:
    model: ai/llama3.1
    runtime_flags:
      - "--no-prefill-assistant"
